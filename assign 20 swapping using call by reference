
#include <stdio.h>

void swapr(int *a, int *b) {
    int temp = *a;
    *a = *b;     
    *b = temp;    
}

int main() {
    int x = 10, y = 20;
    printf(" x = %d, y = %d\n", x, y);
    
  
    swapr(&x, &y);
    
    printf(" = %d, y = %d\n", x, y); 
    return 0;
}
